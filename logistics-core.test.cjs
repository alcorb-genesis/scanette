const test=require('node:test'),assert=require('node:assert/strict'),C=require('./logistics-core.js');
test('CSV preserves uncounted versus zero and protects spreadsheet cells',()=>{const s={id:'id',kind:'inventory',content:{event_at:'2026-09-21T12:00:00Z',employees:['=EXEC()'],lines:[{reference:'=1+1',quantity:null},{reference:'ABC',quantity:0}]}};const csv=C.csv(s);assert.ok(csv.startsWith('\ufeff'));assert.ok(csv.includes('"\'=1+1"'));assert.ok(csv.includes('"\'=EXEC()"'));assert.ok(csv.includes('"ABC";"";"0"'));assert.ok(csv.includes('"\'=1+1";"";""'));});
test('Import copies the original pointage without changing it or adding quantities',()=>{const s={items:{x:{qty:3,prix:null,productId:'uuid',reference:'ABC'}}};const before=JSON.stringify(s);const lines=C.importSection(s);lines[0].quantity=7;assert.equal(JSON.stringify(s),before);assert.equal(C.importSection(s)[0].quantity,3);});
test('Printable receipt escapes supplier, employees and article text',()=>{const html=C.print({id:'id',kind:'receipt',content:{event_at:'2026-09-21T12:00:00Z',supplier_name:'<script>bad</script>',orders:'A&B',lines:[{reference:'<img>',quantity:null}]}});assert.ok(!html.includes('<script>bad'));assert.ok(html.includes('&lt;script&gt;'));assert.ok(html.includes('A&amp;B'));assert.ok(html.includes('window.print()'));});
