const test=require('node:test'),assert=require('node:assert/strict'),S=require('./sales-core'),D=require('./delivery-print');
test('Calcul prix et remise en centimes, arrondi unitaire explicite',()=>{assert.deepEqual(S.line('2','12,34','10'),{quantity:2,base_cents:1234,discount_bp:1000,unit:1111,total:2222});assert.equal(S.line(1,'0.01','50').unit,1);assert.equal(S.line(1,'10','100').total,0);for(const v of ['','NaN','-1','1.234','1e2'])assert.throws(()=>S.line(1,v,'0'));assert.throws(()=>S.line(1,'10','100.01'));assert.throws(()=>S.line(1.5,'10','0'));});
test('BL réel imprimé depuis snapshot et annulation clairement visible',()=>{const s={real:true,id:'BL-1',store:'Magasin',status:'cancelled',createdAt:'2026-09-21T09:00:00Z',client:{name:'<Garage>',code:'C1'},lines:[{ref:'R',name:'Pièce',quantity:2,base_cents:1234,discount_bp:1000,unit:1111}]};const html=D.html(s);assert.ok(html.includes('ANNULÉ'));assert.ok(html.includes('&lt;Garage&gt;'));assert.ok(html.includes('Remise : 10 %'));assert.ok(!html.includes('DÉMONSTRATION'));assert.ok(D.html({...s,real:false}).includes('DÉMONSTRATION'));});
